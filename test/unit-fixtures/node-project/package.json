{
  "name": "project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "cross-env-shell echo \"Error: no test specified\" && exit 1",
    "build": "cross-env-shell echo \"Fake build finished\" && exit 0"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^7.0.2"
  }
}
